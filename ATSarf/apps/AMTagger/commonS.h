#ifndef COMMONS_H
#define COMMONS_H

typedef enum {user, sarf} Source;
typedef enum {original, compareTo} Dest;
typedef enum {tagV, tagTV, sarfTV, sarfTTV, sarfMSF} Data;
typedef enum {common, forward, _reverse} DestText;

#endif // COMMONS_H
